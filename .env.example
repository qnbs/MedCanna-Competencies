# No env vars are needed for the build.
# The Gemini API key is entered at runtime in Settings → API Key.
# This file exists only for documentation purposes.

# (Legacy – no longer used in Vite build)
# GEMINI_API_KEY=your_key_here
